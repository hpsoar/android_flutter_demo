include ':app', ':flutter_utils', ':core', ':widgets'

// setup flutter
setBinding(new Binding([gradle: this]))

// path to flutter android config
evaluate(new File(
        settingsDir,
        'flutter_demo/.android/include_flutter.groovy'
))
