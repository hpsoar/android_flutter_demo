<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.lattern.flutter_utils">

    <application>
        <activity android:name=".CYFlutterActivity">
            <intent-filter>
                <action android:name="android.intent.action.RUN"></action>
            </intent-filter>
        </activity>
    </application>

</manifest>
